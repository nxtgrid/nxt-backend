import { Injectable } from '@nestjs/common';

/**
 * Metering command/interaction shell. Product HTTP and the device-messaging
 * client land in later tasks of this slice.
 */
@Injectable()
export class MeterInteractionsService {}
